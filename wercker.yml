box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        theme: slim
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/master@0.2.1:
        token: $GIT_TOKEN
        domain: https://sn2562.github.io/blog/
        basedir: public
